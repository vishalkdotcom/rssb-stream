<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RssbStream" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash screen theme can still extend Theme.SplashScreen, but ensure it transitions to the correct base theme -->
    <!-- For dark mode, postSplashScreenTheme should ideally point to a dark version of Theme.RssbStream if specific overrides are needed -->
    <!-- However, since Theme.RssbStream itself will be resolved from values-night, this setup should work. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
<!--        <item name="windowSplashScreenBackground">@color/rssb_stream_purple_dark</item>-->
<!--        &lt;!&ndash; icon and animation can be the same or specific for dark theme &ndash;&gt;-->
<!--        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>-->
<!--        <item name="postSplashScreenTheme">@style/Theme.RssbStream</item>-->
    </style>
</resources>
